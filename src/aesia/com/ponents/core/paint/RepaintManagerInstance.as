/**
 * @license
 */
package aesia.com.ponents.core.paint 
{
	/**
	 * Instance globale de la classe <code>RepaintManager</code>.
	 * <p>
	 * Cette instance est utilisée par tout les composants descendant
	 * de la classe <code>AbstractComponent</code> pour la gestion de leur
	 * redessin.
	 * </p>
	 */
	public const RepaintManagerInstance : RepaintManager = new RepaintManager();
}
